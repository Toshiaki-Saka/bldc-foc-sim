# PI ゲインの極配置設計

`bldc-foc-sim` の dq 軸 PI 制御器のゲインをどう決めるかを説明する。コード上は `sim_params.hpp` の設計パラメータと `main.cpp` のゲイン算出式に対応する。

---

## 1. 設計の考え方

本コードでは、PI ゲイン $K_p$・$K_i$ を直接調整しない。代わりに、閉ループの応答特性を表す 2 つのパラメータだけを設計者が決める。

| パラメータ | 記号 | 意味 |
|------------|------|------|
| 自然角周波数 | `kWn` ($\omega_n$) | 応答の速さ |
| 減衰比 | `kZeta` ($\zeta$) | オーバーシュートの量 |

この 2 つから `main.cpp` が $K_p$・$K_i$ を自動算出する。「応答特性を指定すれば、ゲインは数式が決める」という設計思想である。

---

## 2. 制御ループの構造

電気系のプラント (電圧 → 電流の伝達関数) は 1 次遅れ系である。

$$
G(s) = \frac{1}{Ls + R}
$$

これに PI 制御器を組み合わせる。

$$
C(s) = K_p + \frac{K_i}{s} = \frac{K_p s + K_i}{s}
$$

閉ループ伝達関数は次のようになる。

$$
T(s) = \frac{C(s) G(s)}{1 + C(s) G(s)}
$$

分母を整理すると、

$$
\text{分母} = s^2 + \frac{R + K_p}{L} s + \frac{K_i}{L}
$$

---

## 3. 標準二次系との一致

二次系の標準形は次の形である。

$$
s^2 + 2\zeta\omega_n s + \omega_n^2
$$

閉ループの分母をこの標準形に一致させる (極配置)。

$$
\frac{R + K_p}{L} = 2\zeta\omega_n \quad \Rightarrow \quad K_p = 2\zeta\omega_n L - R
$$

$$
\frac{K_i}{L} = \omega_n^2 \quad \Rightarrow \quad K_i = \omega_n^2 L
$$

これが本コードのゲイン算出式である。`main.cpp` には次のように実装されている。

```cpp
const double kKp = 2.0 * kZeta * kWn * kL - kR;
const double kKi = kWn * kWn * kL;
```

---

## 4. パラメータの決め方

### Step 1 — kWn (自然角周波数) を決める

`kWn` は電流ループの応答速度を決める。目安は **電気時定数の逆数の数倍**。

- 電気時定数 $\tau_e = L/R$。本コードでは $L = 0.1\,\mathrm{mH}$、$R = 0.1\,\Omega$ なので $\tau_e = 1\,\mathrm{ms}$、その逆数は $1000\,\mathrm{rad/s}$
- `kWn` はこの 1〜10 倍程度に置く。本コードは `kWn = 1000 rad/s` を採用
- **上限の制約**: サンプリング周期 $T_s = 250\,\mu\mathrm{s}$ に対し $\omega_n T_s < 0.3$ が目安。$1000 \times 0.00025 = 0.25$ で許容範囲。これを超えると離散化誤差が大きくなり発散しうる

### Step 2 — kZeta (減衰比) を決める

`kZeta` は閉ループ応答のオーバーシュート量を決める。

| kZeta | 挙動 |
|-------|------|
| 1.0 | 臨界制動。オーバーシュートなし・最速整定 (002 モデルが採用) |
| 0.7〜0.8 | 微小オーバーシュート。実機の応答に近い (003/004 モデルが採用) |
| < 0.7 | オーバーシュート大。整定に振動が残る |
| > 1.0 | 過制動。応答が鈍くなる |

EPS のように滑らかさを重視するなら 1.0 寄り、応答性を重視するなら 0.7 寄りに置く。

---

## 5. 算出されたゲインの物理的意味

- **$K_p = 2\zeta\omega_n L - R$** : 比例ゲイン。プラントの極 $-R/L$ を打ち消し、応答を速める働きをする
- **$K_i = \omega_n^2 L$** : 積分ゲイン。定常偏差をゼロにする働きをする

$\zeta = 1$、$\omega_n = 1000\,\mathrm{rad/s}$ のとき:

$$
K_p = 2 \times 1 \times 1000 \times 0.0001 - 0.1 = 0.1
$$

$$
K_i = 1000^2 \times 0.0001 = 100
$$

これらは `motor_controller.cpp` の `PidController` で $K_p e_p + K_i e_i$ として使われる (`e_p` は比例偏差、`e_i` は台形積分した積分偏差)。

---

## 6. 検証 — 波形から正しさを判断する

- **立ち上がり時間** $\approx \dfrac{5}{\zeta \omega_n}$。$\zeta=1,\, \omega_n=1000$ なら約 5 ms で整定
- **オーバーシュート率** $= \exp\!\left(-\dfrac{\pi\zeta}{\sqrt{1-\zeta^2}}\right)$。$\zeta=0.8$ で約 1.5 %
- `data/motor_log.csv` の `iq` 列・`omega` 列の波形で立ち上がり・整定を確認する
- `csv_verifier` でリファレンス CSV と差分照合し、想定からのズレを検出する

---

## 7. チューニングが失敗するケース

| 症状 | 原因 | 対処 |
|------|------|------|
| $K_p$ が負になる | $2\zeta\omega_n L < R$ の領域 | `kWn` を上げる |
| iq が発散・振動 | `kWn` が高すぎ ($\omega_n T_s > 0.3$) | `kWn` を下げる |
| 整定が遅すぎる | `kWn` が低い、または B 型では PWM 電圧飽和が律速 | `kWn` を上げる。飽和が原因なら $V_{dc}$ 側を見直す |
| 定常偏差が残る | 積分が効いていない | $K_i$ と積分偏差の蓄積を確認 |

---

## 関連ドキュメント

- [`foc.md`](foc.md) — ベクトル制御 (FOC) の原理
- [`motor-model.md`](motor-model.md) — モータの電気・機械方程式
